package com.adorsys.fineract.asset.bdd.steps;

/**
 * Security-specific step definitions.
 * Most security steps are handled by CommonStepDefinitions (unauthenticatedUserCalls,
 * userWithRoleCalls, responseStatusShouldBe). This class exists as a placeholder
 * for any future security-specific steps beyond basic auth/RBAC.
 */
public class SecurityStepDefinitions {
    // Authentication and authorization steps are handled by CommonStepDefinitions.
    // Security-specific assertions (e.g. token extraction, claim checks) can be added here.
}
