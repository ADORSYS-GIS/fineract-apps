package com.adorsys.fineract.asset.dto;

/** Direction of a trade. */
public enum TradeSide {
    /** User is purchasing asset units with settlement currency. */
    BUY,
    /** User is selling asset units for settlement currency. */
    SELL
}
