package com.adorsys.fineract.asset.dto;

/** Classification category for an asset. */
public enum AssetCategory {
    /** Tokenized real estate properties. */
    REAL_ESTATE,
    /** Physical commodities (e.g. gold, oil). */
    COMMODITIES,
    /** Agricultural products and farmland tokens. */
    AGRICULTURE,
    /** Equity shares and stock indices. */
    STOCKS,
    /** Cryptocurrency and digital assets. */
    CRYPTO
}
