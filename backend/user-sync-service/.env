# Keycloak Admin API Configuration
# Copy this file to .env.local and update with real values for local development
KEYCLOAK_URL=http://localhost:9000
KEYCLOAK_REALM=fineract
KEYCLOAK_CLIENT_ID=setup-app-client

# IMPORTANT: Store actual secret in .env.local (not committed)
# In Kubernetes, this is injected via Secret mount
KEYCLOAK_CLIENT_SECRET=**********

# Service Configuration
PORT=5000
